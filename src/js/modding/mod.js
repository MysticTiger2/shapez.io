export class ModBase {
    constructor(auth, id, modid, target, version) {
        this.author = auth;
        this.name = id;
        this.modid = modid;
        this.target_version = target;
        this.version = version;
    }
    //this class/file is depreciated NOW so delete it, ty


}